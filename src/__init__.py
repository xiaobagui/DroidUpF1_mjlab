"""External robot assets and tasks built on top of mjlab."""

from pathlib import Path

SRC_PATH = Path(__file__).resolve().parent
PROJECT_ROOT = SRC_PATH.parent
DATASET_PATH = PROJECT_ROOT / "dataset"
